# Prompt Log — Лабораторная работа №13

## Задание 1 — Определение агентов и их ролей

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Ты — архитектор мультиагентных систем. Предметная область: бронирование гостиниц. Выдели не менее 4 типов агентов. Для каждого опиши: роль, тип агента (реактивный/целевой/BDI), входные данные (JSON-поля), выходные данные, бизнес-правила с граничными случаями. Также опиши схему взаимодействия агентов через оркестратор и укажи паттерн взаимодействия (pipeline/blackboard/auction)."  
**Результат:** Получила описание 4 агентов (SearchAgent, BookingAgent, PaymentAgent, CancellationAgent) с полными спецификациями, бизнес-правилами и схемой взаимодействия через оркестратор по паттерну pipeline.

---

## Задание 2 — Разработка прототипа агента на Go

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Реализуй SearchAgent на Go для предметной области 'бронирование гостиниц'. Агент подписывается на топик hotel.search.request в NATS, принимает JSON с полями city, check_in, check_out, guests, room_type, max_price. Реализуй: валидацию входных данных с конкретными бизнес-правилами, in-memory базу из 6 номеров в разных городах с существующими бронями, алгоритм проверки доступности через пересечение временных интервалов, фильтрацию по вместимости и цене. Публикуй результат в hotel.search.result и ошибки в hotel.search.error. Добавь graceful shutdown."  
**Результат:** Получила полностью рабочий агент с валидацией, in-memory базой, алгоритмом проверки пересечения интервалов, публикацией в два разных топика и graceful shutdown через SIGINT/SIGTERM.

### Промпт 2
**Инструмент:** Claude  
**Промпт:** "Напиши юнит-тесты для SearchAgent на Go без запуска NATS. Покрой: валидацию всех бизнес-правил (граничные значения дат, нулевые гости, неверный формат), алгоритм isRoomAvailable для случаев полного совпадения, частичного пересечения и смежных периодов, функцию searchRooms с фильтрами по городу, вместимости, цене и типу номера, корректность подсчёта TotalPrice за несколько ночей."  
**Результат:** Получила 12 тестов покрывающих все критичные сценарии, включая граничный случай со смежными периодами бронирования.

---

## Задание 3 — Разработка оркестратора на Python

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Разработай асинхронный оркестратор на Python с asyncio и nats-py для управления SearchAgent. Реализуй класс HotelOrchestrator с методами connect, disconnect, search_rooms. Используй asyncio.Future для ожидания ответов агента с таймаутом. Продемонстрируй 4 сценария: успешный поиск, фильтрация по цене, невалидный запрос, таймаут. Добавь метрики: счётчики отправленных задач, успешных, ошибок и таймаутов."  
**Результат:** Получила оркестратор с корректной схемой request-reply через Future, обработкой таймаута и всеми 4 сценариями в main(). Все сценарии отработали корректно при первом запуске.

---

## Задание 5 — Логирование и мониторинг

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Добавь структурированное логирование в SearchAgent (Go) и оркестратор (Python). Требования: уровни INFO и ERROR, одновременная запись в консоль и файл через MultiWriter (Go) и logging.FileHandler (Python), атомарные счётчики через sync/atomic в Go. В каждом лог-сообщении указывай имя агента, ID задачи, текущие метрики. При остановке агента выводи итоговую статистику."  
**Результат:** Получила реализацию с io.MultiWriter в Go и dual-handler логгером в Python. Логи пишутся в search-agent.log и orchestrator.log одновременно с выводом в консоль.

---

## Задание 6 — Обработка ошибок и таймаутов

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Добавь в оркестратор механизм retry с экспоненциальной задержкой. При таймауте агента повторяй задачу до max_retries раз (по умолчанию 3). Задержка между попытками: attempt * 2 секунды. Каждая попытка должна генерировать новый task_id. Логируй номер попытки и время ожидания. После исчерпания попыток бросай TimeoutError с информативным сообщением."  
**Результат:** Получила реализацию retry с нарастающей задержкой. Каждая попытка логируется с номером и task_id. После 3 неудачных попыток выбрасывается TimeoutError.

---

## Задание 7 — Запуск нескольких агентов

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Настрой балансировку нагрузки между 3 экземплярами SearchAgent через NATS queue groups. Замени nc.Subscribe на nc.QueueSubscribe с группой 'search-agents'. Передавай имя агента через переменную окружения AGENT_NAME и включай его в лог-сообщения. Обнови docker-compose.yml: запусти 3 сервиса search-agent-1/2/3 с разными AGENT_NAME. Добавь в оркестратор 9 параллельных запросов через asyncio.gather для демонстрации распределения."  
**Результат:** Получила рабочую балансировку — все 9 запросов выполнены параллельно, в логах docker-compose видно распределение задач между agent-1, agent-2, agent-3.

---

## Задание 8 — REST API

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Создай REST API на FastAPI поверх оркестратора. Реализуй: GET /health для проверки состояния, POST /search принимает SearchRequest (Pydantic-модель) и возвращает SearchResponse. Используй lifespan или on_event для подключения к NATS при старте. При таймауте агента возвращай HTTP 504. Включи автодокументацию через Swagger UI."  
**Результат:** Получила рабочий FastAPI-сервис. Запрос через curl вернул корректный JSON с найденными номерами. Swagger UI доступен на /docs.

---

## Задание 9 — Тестирование системы

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Напиши pytest-тесты для оркестратора с моками NATS. Не используй реальное подключение к брокеру — мокай nc.publish через AsyncMock. Для имитации ответа агента в fake_publish сразу резолви Future с нужным результатом. Покрой: успешный поиск, ошибку от агента, таймаут с одной попыткой, retry с 3 попытками (проверь call_count publish), метрики при успехе и при ошибке."  
**Результат:** Получила 7 тестов без зависимости от NATS. Все тесты прошли, включая проверку что retry делает ровно 3 вызова publish.

### Промпт 2
**Инструмент:** Claude  
**Промпт:** "Напиши pytest-тесты для FastAPI эндпоинтов с использованием httpx.AsyncClient и ASGITransport. Покрой: GET /health, POST /search с успешным ответом агента, POST /search с ошибкой валидации от агента, POST /search при HTTP 504 (мокай search_rooms через patch.object с side_effect=HTTPException), POST /search с невалидным телом запроса (422)."  
**Результат:** Получила 5 тестов для API. 4 из 5 прошли сразу, один потребовал замены Exception на HTTPException в side_effect.

---

## Задание 10 — Документирование архитектуры

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Создай файл ARCHITECTURE.md с двумя Mermaid-диаграммами: graph TD для взаимодействия компонентов (HTTP Client → FastAPI → Orchestrator → NATS → 3 агента → In-Memory DB) и sequenceDiagram для полного цикла запроса с нотами о retry и балансировке. Добавь описание каждого компонента: роль, порты, топики NATS, бизнес-правила. Включи таблицу паттернов взаимодействия и структуру проекта."  
**Результат:** Получила полный файл с двумя диаграммами, описанием всех компонентов и таблицей паттернов.

---

## Итого

- **Количество промптов:** 11
- **Что пришлось исправлять вручную:** замена `Exception` на `HTTPException` в тесте таймаута, перенос объявления `agentName` в начало `main()` в Go, версии зависимостей в Dockerfile
- **Время:** ~3 часа
